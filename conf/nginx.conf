daemon off;

#user  nobody;
worker_processes  1;

error_log /dev/stdout info;
#error_log  logs/error.log info;

#error_log  logs/error.log  notice;
#error_log  logs/error.log  info;

#pid        logs/nginx.pid;


events {
    worker_connections  1024;
}


http {
    include       mime.types;
    default_type  application/octet-stream;

    #log_format  main  '$remote_addr - $remote_user [$time_local] "$request" '
    #                  '$status $body_bytes_sent "$http_referer" '
    #                  '"$http_user_agent" "$http_x_forwarded_for"';

    #access_log  logs/access.log  main;

    sendfile        on;
    #tcp_nopush     on;

    #keepalive_timeout  0;
    keepalive_timeout  65;

    #gzip  on;

    kafka_log_rdkafka_property bootstrap.servers 143.244.58.132:9092;
    kafka_log_rdkafka_property client.id example;
    kafka_log_rdkafka_property compression.codec none;
    kafka_log_rdkafka_property message.send.max.retries 2147483647;
    kafka_log_rdkafka_property queue.buffering.max.messages 48000;
    kafka_log_rdkafka_property retry.backoff.ms 100;
    kafka_log_rdkafka_property debug eos;
    kafka_log_rdkafka_property log_level 1;

    #kafka_log_kafka_brokers 143.244.58.132:9092;
    #kafka_log_kafka_client_id example;
    #kafka_log_kafka_compression none;
    #kafka_log_kafka_max_retries 2147483647;
    #kafka_log_kafka_buffer_max_messages 48000;
    #kafka_log_kafka_backoff_ms 100;
    #kafka_log_kafka_debug eos;
    #kafka_log_kafka_log_level 1;

    kafka_log_enable on;

    server {
        listen       80;
        server_name  localhost;

        #charset koi8-r;

        #access_log  logs/host.access.log  main;

        location / {
            root   html;
            index  index.html index.htm;

            kafka_log kafka:kaltura "$msec $host $uri";
        }

        #error_page  404              /404.html;

        # redirect server error pages to the static page /50x.html
        #
        error_page   500 502 503 504  /50x.html;
        location = /50x.html {
            root   html;
        }

        # proxy the PHP scripts to Apache listening on 127.0.0.1:80
        #
        #location ~ \.php$ {
        #    proxy_pass   http://127.0.0.1;
        #}

        # pass the PHP scripts to FastCGI server listening on 127.0.0.1:9000
        #
        #location ~ \.php$ {
        #    root           html;
        #    fastcgi_pass   127.0.0.1:9000;
        #    fastcgi_index  index.php;
        #    fastcgi_param  SCRIPT_FILENAME  /scripts$fastcgi_script_name;
        #    include        fastcgi_params;
        #}

        # deny access to .htaccess files, if Apache's document root
        # concurs with nginx's one
        #
        #location ~ /\.ht {
        #    deny  all;
        #}
    }


    # another virtual host using mix of IP-, name-, and port-based configuration
    #
    #server {
    #    listen       8000;
    #    listen       somename:8080;
    #    server_name  somename  alias  another.alias;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}


    # HTTPS server
    #
    #server {
    #    listen       443 ssl;
    #    server_name  localhost;

    #    ssl_certificate      cert.pem;
    #    ssl_certificate_key  cert.key;

    #    ssl_session_cache    shared:SSL:1m;
    #    ssl_session_timeout  5m;

    #    ssl_ciphers  HIGH:!aNULL:!MD5;
    #    ssl_prefer_server_ciphers  on;

    #    location / {
    #        root   html;
    #        index  index.html index.htm;
    #    }
    #}

}
